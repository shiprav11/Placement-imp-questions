#include <bits/stdc++.h>
using namespace std;

int smallest(int arr[],int n,int k)
{
    sort(arr,arr+n);
    return arr[k-1];
}
int main()
{
    int arr[]={12,6,34,87,54};
    int n=sizeof(arr)/sizeof(arr[0]),k=2;
    cout<<smallest(arr,n,k);
    return 0;
}

